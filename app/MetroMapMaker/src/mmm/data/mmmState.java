package mmm.data;

/**
 *
 * @author myungsuk
 */
public enum mmmState {
    SELECTING_MODE,
    DRAGGING_MODE,
    ADD_LINE_MODE,
    ADD_LABEL_MODE,
    ADD_IMAGE_MODE,
    ADD_STATION_MODE,
    ADD_STATION_TO_LINE_MODE,
    REMOVE_STATION_FROM_LINE_MODE,
    DRAGGING_NOTHING,
    SIZING_NOTHING
}
